{
  "name": "edge-ship1-node",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ship 1 assignment template for Node/JS at Edge Labs",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "lint": "eslint .",
    "format": "prettier --write .",
    "preflight": "node scripts/preflight.mjs",
    "validate": "npm run preflight"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "eslint": "^8.52.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-jest": "^27.2.0",
    "jest": "^29.6.4",
    "prettier": "^3.0.3"
  }
}